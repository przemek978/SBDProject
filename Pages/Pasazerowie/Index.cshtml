@page
@model SBD.Pages.Pasazerowie.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Pasazer[0].nazwisko)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Pasazer[0].imie)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Pasazer) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.nazwisko)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.imie)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.id_pasazera">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.id_pasazera">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.id_pasazera">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
