@page
@model SBD.Pages.Bagaze.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Bagaz[0].waga)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Bagaz) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.waga)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.id_bagazu">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.id_bagazu">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.id_bagazu">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
