@page
@model SBD.Pages.Bagaze.IndexModel

@{
    ViewData["Title"] = "Bagaże";
}

<h1>Bagaże</h1>

<p>
    <a class="btn button button4" asp-page="Create" class="btn button button1">Dodaj</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Bagaz[0].waga)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Bagaz) {
        <tr>
            <td>
                    @Html.DisplayFor(modelItem => item.waga) kg
            </td>
            <td>
                <a class="btn button button4" asp-page="./Edit" asp-route-id="@item.id_bagazu">Edytuj</a> |
                <a class="btn button button4" asp-page="./Details" asp-route-id="@item.id_bagazu">Szczegóły</a> |
                <a class="btn button button4" asp-page="./Delete" asp-route-id="@item.id_bagazu">Usuń</a>
            </td>
        </tr>
}
    </tbody>
</table>
